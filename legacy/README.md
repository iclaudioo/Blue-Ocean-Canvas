# Legacy single-file version

The original self-contained Blue Ocean Strategy Canvas (vanilla HTML/CSS/JS, no build step).
Kept for reference; the repository root now hosts the Next.js app that replaced it.
Open `index.html` directly in a browser to use it.
